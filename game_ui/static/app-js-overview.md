# `game_ui/static/` — frontend architecture overview (ES modules)

> **Contract: this file is the living map of the frontend.** Before any
> non-trivial change to the `game_ui/static/*.mjs` modules, read this file; after
> the change, update it in the same commit (new function / changed control flow /
> new message shape / new UI mode / renamed or removed function / new module /
> changed import graph). Trivial fixes (typo, debug line, CSS class tweak) do not
> require a doc update.

The frontend is a set of **vanilla ES modules** (no framework; `import`/`export`,
`"use strict"`). The entry point is **`app.mjs`**, loaded from `index.html` via
`<script type="module" src="/static/app.mjs">`. Importing `app.mjs` pulls the whole
dependency graph; `boot()` (in `setup.mjs`) wires the setup page and loads the card
pools. The old monolithic `app.js` was split into these modules — each file is now
self-contained and owns exactly one concern.

It drives the whole web UI of the game (served by `game_ui/app.py` on port 8001):
the setup/launch page, the game page (board, zones, trip chain, log) and the two-way
communication with the engine.

## Module map

| Module | Responsibility | Key exports |
|---|---|---|
| `utils.mjs` | Tiny DOM/fetch helpers shared by everything | `$`, `$$`, `toast`, `api` |
| `cards.mjs` | Card data (main + support pools), lookup helpers, UI constants, deck-size constants, `buildSupportDeck` | `CARDPOOL`, `SUPPORT`, `loadCardpool`, `loadSupportCards`, `cardInfo`, `cardTitle`, `cardImg`, `cardCost`, `cardName`, `earthBgSrc`, `ENGINEER_DROPS`, `ENGINEER_DWELLING`, `isEngineerDrop`, `isEngineerDwelling`, `DOCTOR_PENDING`, `DOCTOR_DWELLING`, `isDoctorPending`, `isDoctorDwelling`, `MAGE_CELASTIAL_REVERSAL`, `isMageCelestial`, `MAGE_NOBODYMOVES`, `isMageNobodyMoves`, `MAGE_THERMIC_FLUX`, `isMageThermicFlux`, `MAGE_APOCALYPTICRITUAL`, `isMageApocalypticritual`, `MAGE_BLACK_HOLE`, `isMageBlackHole`, `FACTIONS`, `SUPPORT_FACS`, `getFactionKey`, `dwellingPlaceholderSrc`, `factionLogoSrc`, `buildSupportDeck`, `MAIN_DECK_SIZE`, `SUPPORT_DECK_SIZE` |
| `phase.mjs` | Phase detection (parses the engine `state` string) + whose turn | `detectPhase`, `myTurn`, `START_MANA_N` |
| `game.mjs` | Game-page state object, engineer cell-selection mode, `enterGame` | `game`, `cellSelect`, `enterCellSelect`, `exitCellSelect`, `confirmCell`, `enterGame` |
| `comm.mjs` | WebSocket + polling + the `applyState` render funnel | `connectWs`, `sendAction`, `setConn`, `startPolling`, `stopPolling`, `applyState`, `wsUrl` |
| `state.mjs` | `renderAll` + the client-side mana guard | `checkMana`, `renderEnv`, `highlightValidCells`, `renderAll` |
| `zones.mjs` | Zone panels & side rows (hand, mana backs, dwelling/pendings/deck/discard) — the two **discard side-panels are clickable** (open the pile viewer, wired in `modals.mjs`) | `publicCount`, `renderOppZone`, `renderMyZone`, `renderSideRows`, `makeStaticCard` (also toggles the `#dz-mana.mana-waiting` purple glow) |
| `board.mjs` | The Earth ring + stopover slots + tokens/drops/markers | `BIOME_NAMES`, `N_CELLS`, `N_STOPOVERS`, `slotEls`, `POS24`, `buildBoard`, `renderBoard` |
| `actions.mjs` | Stopover ordering (**per-player** positions) + the unified play dispatch | `playedCount`, `rootedCount`, `nextPosition`, `occupiedCols`, `freeCols`, `nextSlotCol`, `actionStopoverNum`, `canDropOnStopover`, `playCardToStopover`, `orderHint`, `dispatchPlay` |
| `interaction.mjs` | The hand card element (click-select, drag & drop, playable outline) | `makeHandCard` |
| `anim.mjs` | Card-movement ("where do my cards go") flight animation | `FLY_MS`, `FLY_STAGGER_MS`, `REDUCED_MOTION`, `snapshotZones`, `planPlayerMoves`, `flyCard`, `flipMyHand`, `popNewBoardTokens`, `animateZoneTransitions` |
| `theme.mjs` | **Per-faction UI theme colors** — loads `static/faction_themes.json` (editable, no-cache) and applies each MAIN faction's palette as CSS variables (`--accent` / `--accent-2` / `--accent-contrast`, opponent variant `--oppo-*`): setup page (selected faction, hover previews a palette), game page (my palette = page default, opponent's palette on `#oppo-zone` / `#oppo-side` / `#row-oppo` + their board marker). All tints/glows in `style.css` derive from these vars with `color-mix()`; the page **background is never themed** | `THEME_DEFAULTS`, `loadFactionThemes`, `factionTheme`, `applyFactionTheme`, `applyOpponentTheme`, `syncGameThemes` |
| `bg.mjs` | **Setup-page animated background** — random cards (main + support art, **9-bucket equal-faction distribution**: 1/9 each for the 6 main + 3 support factions) floating up the screen behind the setup page at low opacity, all knobs driven by the JSON file `static/background_cards_parameters.json` (spawn rate, rise speed, size + jitter, rotation speed, starting-rotation range, opacity, spread, max on screen); started from `setup.mjs` `boot()`, stopped (and the `#bg-cards` layer removed) by `game.mjs` `enterGame()`, **restarted by `modals.mjs` `leaveGame()`** ("Back to home" / "✕ Leave") — the background is **setup-page only**; **hovering a background card freezes it and shows the 3x preview** (`dataset.hoverId` + the delegated `modals.mjs` hover, selector `.card, .bg-card`; CSS: `#view-setup` is `pointer-events:none` with `button/input/label/a/.card` re-enabled (the setup page's external links, e.g. the deck-builder panel), `.bg-card:hover` brightens) | `startBgCards`, `stopBgCards` |
| `log.mjs` | The game-log panel renderer | `effectLabel`, `stopoverLabel`, `makeLogCardEl`, `makeTag`, `buildInstantEl`, `buildLogEntryEl`, `buildLogTurnEl`, `fitLogWidth`, `renderLog` |
| `chainanim.mjs` | Trip-chain resolution animation (slot pulses + thread) | `clearChainAnim`, `placeThread`, `playChainAnim` |
| `modals.mjs` | Hover preview (**with a condition/effect legend under the image** — `hoverCardText` + `COND_TOOLTIPS`/`EFF_TOOLTIPS`, "X" = the card's `effect_number` magnitude; support cards show their pool `description`; hidden for card backs), card zoom modal, **discard-pile viewer** (my + opponent pile), server message, endgame, leave | `showCardHover`, `hideCardHover`, `showCardModal`, `showDiscardPilePopup`, `refreshDiscardPilePopup`, `showServerMsg`, `showEndgame`, `leaveGame` |
| `setup.mjs` | Setup page (deck building, factions, launch, waiting) + `boot()` (which also starts the card background — `bg.mjs`, and loads the faction themes JSON — `theme.mjs`). Picking a faction **themes the setup page with that faction's colors** (`setSetupTheme`); hovering a faction button previews its palette, mouseleave restores the selection. **Loading a text-file deck also themes the page** — the handler detects the loaded deck's dominant faction (majority of the cards) and calls `setSetupTheme` with it, so the setup palette follows the file. **Faction tooltips**: every main + support faction button carries a hover tooltip with a short role description (`addFactionTip`, text in `FACTION_TIPS`, support colors in `SUPPORT_TIP_COLORS`; the tooltip background = the faction's OWN color — `factionTheme(name)` for the mains, banner color for the supports; CSS `.faction-tip` in `style.css`, revealed by pure `:hover`) | `setup`, `initSetup`, `boot` |
| `app.mjs` | **Entry point** — imports `setup.mjs` (pulls the graph) and calls `boot()` | — (side effect) |

### Import graph (safe circularity)

The modules are **heavily cross-importing** and the graph is **circular** (e.g.
`game ↔ comm ↔ state`, `actions ↔ board ↔ zones`, `setup ↔ game`). This is safe in
ES modules **because no module reads another module's `const` at top level** — every
cross-module reference is inside a **function body** (evaluated lazily, after the
whole graph has finished evaluating). Top-level statements are limited to:
self-contained `const` initializers, DOM wiring in `modals.mjs` (the `<script>` is
deferred, so the DOM is ready), a `resize` listener in `log.mjs`, and the `boot()`
call in `app.mjs`. **Keep it that way:** if you add a top-level statement that reads
an imported `const`, you can hit a TDZ error — move it inside a function.

## Ground rules it mirrors from the engine

These invariants are **re-implemented client-side** — keep them in sync with the
engine (`engine/game_engine.py`) and with `game_ui/ai_driver.py`:

| Invariant | Where |
|---|---|
| Player message shape `{cards, to, mode, pendings[, cell]}` | `comm.mjs` `sendAction()` |
| Card cost = main card `mana`, support card `mana_cost` | `cards.mjs` `cardCost()` + `state.mjs` `checkMana()` |
| Stopover ordering: **PER-PLAYER** — each player has their OWN 5 positions (1..5, columns 4,3,2,1,0). A player's rooted cards occupy the leading positions of that player's chain (position 1, 2, …) and the player's plays (move OR defend) go AFTER the rooted cards (position R+1, …). A player's rooted cards NEVER shift the opponent's positions. **The dwelling placeholder ALSO consumes one position** (the engine increments `play_count` when the refinery is placed, and `playedCount()` counts it via `p.dwelling && p.dwelling_slot != null`) so the next play lands on the position AFTER the placeholder | `actions.mjs` `playedCount()`, `rootedCount()`, `nextPosition()`, `freeCols()`, `nextSlotCol()`, `canDropOnStopover()` — mirrors `ge._player_stopover` / `ge._player_chain` (engine = single source of truth; the robot calls it directly in `ai_driver.py`) |
| Only 1 or 3 cards per mana message during init → init sends one at a time | `actions.mjs` `dispatchPlay()`, `init-mana` branch |
| Hidden info: opponent only seen via `*_count` and masked ids — **the discard piles of BOTH players are PUBLIC** (only hand/mana/deck are masked by `current_game_json`) — that's what the discard-pile viewer shows | `zones.mjs` `publicCount()`, `renderOppZone()` (card backs); `modals.mjs` `showDiscardPilePopup()` (read-only pile viewer, no message sent) |
| Deck = 20 main + 10 support, shuffled together before sending | `setup.mjs` `launch()` (`shuffleDeck`) |
| Duplicate main card ids rejected at import | `setup.mjs` `parseCsv` + text-file `onchange` toast |
| Discard selection: `discard`/`discard_oppo` pause the chain — the discarding player picks exactly N hand cards, sent as `{cards:[…N…], to:"discard_pile", mode:""}`; only the discarding player may answer | `phase.mjs` `detectPhase` (`kind:"discard"`), `actions.mjs` **`showDiscardPopup(n)`** (hand-card grid + red DISCARD button at the bottom), `state.mjs` `renderAll` (auto-open + sweep), `interaction.mjs` `makeHandCard` (click = reopen the popup) |

## Global state (now module-scoped)

State lives in the owning module and is imported by its consumers (no `window.*`):

| Object | Module | Contents |
|---|---|---|
| `CARDPOOL` / `cardpoolLoaded` | `cards.mjs` | main card pool: `card_id -> row` (fetched once from `GET /cardpool`) |
| `SUPPORT` / `supportLoaded` | `cards.mjs` | support cards: `card_name -> row` (fetched once from `GET /support_factions`) |
| `FACTIONS` / `SUPPORT_FACS` | `cards.mjs` | the 6 main factions (key, name, logo) + `getFactionKey(name)` + `dwellingPlaceholderSrc(name)` (→ `/cards_ex/placeholder_<key>.png`); engineers/mages/doctors (banner) |
| `ENGINEER_DROPS`, `ENGINEER_DWELLING` | `cards.mjs` | UI constants for the 4 engineer drop cards (image + label) + the `refinery` dwelling card |
| `DOCTOR_PENDING`, `DOCTOR_DWELLING` | `cards.mjs` | UI constants for the 4 doctor pending cards (label only) + the `laboratory` dwelling card |
| `MAGE_CELASTIAL_REVERSAL`, `isMageCelestial` | `cards.mjs` | the Mages' `Celestial_reversal` card + a guard `isMageCelestial(id)`; included in `isSupportPlay` (so it is excluded from the defend button like all support cards) |
| `MAGE_NOBODYMOVES`, `isMageNobodyMoves` | `cards.mjs` | the Mages' `nobodymoves` card + a guard `isMageNobodyMoves(id)`; included in `isSupportPlay` (so it is excluded from the defend button like all support cards). Played in **MOVE mode** like a normal card — no popup, no special dispatch branch (it falls through to the normal move-card fallback in `dispatchPlay`) |
| `MAGE_THERMIC_FLUX`, `isMageThermicFlux` | `cards.mjs` | the Mages' `thermic_flux` card + a guard `isMageThermicFlux(id)`; included in `isSupportPlay` (so it is excluded from the defend button like all support cards). Played in **MOVE mode** but needs a **+4/−4 °C choice** (a popup — see the dispatch table) |
| `MAGE_APOCALYPTICRITUAL`, `isMageApocalypticritual` | `cards.mjs` | the Mages' `Apocalypticritual` card + a guard `isMageApocalypticritual(id)`; included in `isSupportPlay` (so it is excluded from the defend button like all support cards). Played in **MOVE mode** but needs a **cataclysm-order choice** (a popup with 4 ordered biome selectors — see the dispatch table) |
| `MAGE_BLACK_HOLE`, `isMageBlackHole` | `cards.mjs` | the Mages' `black_hole` card + a guard `isMageBlackHole(id)`; included in `isSupportPlay` (so it is excluded from the defend button like all support cards). A **DWELLING** card (like the refinery/laboratory): **placed** in the dwelling zone (`to: "dwelling"`), its effect fires on a **TAP** (`mode: "dwelling_activation"`) which **rotates the earth 3 cells** CW or CCW to the player's choice (a popup — see the dispatch table) |
| `isSwapCards` | `cards.mjs` | a guard `isSwapCards(id)` for the **`swap_cards`** effect — a **MAIN-card** effect (172 cards in the pool), NOT a support card (so it is **not** in `isSupportPlay` and is not excluded from the defend button). Played in **MOVE mode** but with an **OPTIONAL position swap**: `dispatchPlay` opens a **stopover mirror** where the player drag-and-drops the card onto one of their own chain entries (a play / placeholder / rooted card) to exchange positions, or plays without a swap; the chosen position is sent as `swap_with` (a popup — see the dispatch table) |
| `MAIN_DECK_SIZE` / `SUPPORT_DECK_SIZE` | `cards.mjs` | 20 / 10 (deck-size constants) |
| `setup` | `setup.mjs` | setup-page state: `deck` (20 main ids), `supportDeck` (10 support names), `name`, `mode` (`create\|ai\|join`), `faction`, `support`, `gameId` |
| `game` | `game.mjs` | game-page state: `id`, `me` (player name), `ws`, `state` (last **personalized** state), `selected` (Set of hand card ids), `dragCardId`, `pollTimer`, `lastWinnerShown`, `logTurnsRendered` + `logTurnSigs` (incremental log: rendered turn count + per-turn content signature) |
| `cellSelect` | `game.mjs` | engineer drop placement mode: `{active, cardId}` |
| `slotEls` / `POS24` | `board.mjs` | DOM refs to the 2×5 stopover slots + the 24 earth-cell positions (`{x%, y%}`), built once by `buildBoard()` |
| `pendingResolvers` | `comm.mjs` | queue of Promise resolvers — serializes "send action → await reply" over the WS |
| `chainGen` | `chainanim.mjs` | generation counter that invalidates pending trip-chain animation timers |

Helpers: `$` / `$$` (querySelector shorthands), `toast()`, `api()` (fetch + JSON + error unwrap) — all in `utils.mjs`.

## Card data helpers (single lookup path — `cards.mjs`)

All card display goes through these — **never** read `CARDPOOL` directly for a
card that might be a support card:

- `cardInfo(id)` → row from `CARDPOOL` **or** `SUPPORT`, or null.
- `cardTitle(id)` → human title (support cards show `card_name (⚙ faction) — description`).
- `cardImg(id)` → `/art/<card_path>` for support cards, `/art/<card_id>.png` for main cards (the file name differs from the card name for support cards — that's why this helper exists).
- `earthBgSrc(earth)` → the Earth background image for a game, **derived from the board's biome order** (`earth` = `state.earth`, the engine's `biomes_order`): cell 0 is at the top of the ring (clockwise — `POS24`), so segment 0 (cells 0–5) is the image's **top-right quadrant**; the four configs cover one starting biome each — `DE→cfg1, MO→cfg2, OC→cfg3, JU→cfg4` → `/assets/earth_cgf<N>_nomarker.png`. The starting quadrant is always correct; the whole board matches 4/4 when the cyclic order matches the asset (12 of the 24 possible orders — the assets only contain 2 cyclic classes). Falls back to `/assets/earth_background_playmat.png` (the default in `index.html`) when the board is unknown.
- `cardCost(id)` → `mana` or `mana_cost`.
- `cardName(id)` → short name.
- `effectLabel(id)` (in `log.mjs`) → pretty effect label for the log (`advancing +2`, `recoil 1`, …).
- `getFactionKey(factionName)` → short key ("Dwarves" → "Dwa") from the `FACTIONS` table.
- `dwellingPlaceholderSrc(factionName)` → `/cards_ex/placeholder_<key>.png` (faction-specific placeholder art for the dwelling card in the stopover row).
- `factionLogoSrc(factionName)` → `/assets/logo_<key>.png` (the faction logo used as the player token on the Earth ring; `null` if the faction is unknown).

## The three launch branches (`setup.mjs` `launch()`)

| Mode | Request | Then |
|---|---|---|
| `ai` | `POST /create_game_ai` `{name, deck}` | straight into `enterGame()` |
| `create` | `POST /create_game` `{name, deck}` | shows the game id (copyable) + `startWaitingForOpponent()` → `enterGame()` when the 2nd player joins |
| `join` | `POST /join_game/{id}` `{name, deck}` | `enterGame()`; a **409** (already in the game, page reload) is swallowed if the player is present in `GET /game/{id}` |

`deck` is always the **shuffled 30-card mix** (20 main + 10 support) — the server validates it via `check_deck`.

Deck building (`setup.mjs`): `buildStarterDeck` (deterministic per name via FNV-1a
seed + LCG; rare cards weighted 1 vs 3; 20 unique main cards), `parseCsv` (extracts
card_ids from a text file — .txt, or comma/semi/tab separated — one card_id per line,
validates the `XXXn_hex` shape), `shuffleDeck`
(Fisher–Yates). Support deck: `buildSupportDeck` (in `cards.mjs`, 2× each of the 5
unique cards).

## Communication model (important!) — `comm.mjs`

- **WebSocket** (`/ws/{game}/{me}`) is the **only** action channel: `sendAction(cards, to, mode, pendings, cell, dayNight, tempChange, cataclysmOrder, rotation, swapWith)` → Promise. Sends are **serialized** through `pendingResolvers` (one outstanding request). Replies: a game state (applied via `applyState`) or a rejection `{success:false, message}` (toasted via `showServerMsg`). The optional 6th param `dayNight` (`"day"`|`"night"`) is sent as `msg.day_night` and is **only** used by the Mages' `Celestial_reversal` (the player fixes day/night for the rest of the game — `actions.mjs` `showDayNightPopup`); it is omitted for every other action. The optional 7th param `tempChange` (`"up"`|`"down"`) is sent as `msg.temp_change` and is **only** used by the Mages' `thermic_flux` (the player picks +4/−4 °C to change the planet temperature — `actions.mjs` `showThermicFluxPopup`); it is omitted for every other action. The optional 8th param `cataclysmOrder` (a 4-element list of biome codes, a **permutation** of `OC`/`MO`/`DE`/`JU`) is sent as `msg.cataclysm_order` and is **only** used by the Mages' `Apocalypticritual` (the player chooses the order of all 4 cataclysm biomes — `actions.mjs` `showApocalypticritualPopup`); it is omitted for every other action. The optional 9th param `rotation` (`'cw'`|`'ccw'`) is sent as `msg.rotation` and is **only** used by the Mages' `black_hole` **dwelling TAP** (the player chooses the direction to rotate the earth 3 cells — `actions.mjs` `showBlackHoleRotationPopup`); it is omitted for every other action (including the refinery/laboratory dwelling taps, which carry no `rotation`). The optional 10th param `swapWith` (an int 1..5) is sent as `msg.swap_with` and is **only** used by the **`swap_cards`** effect (the player swaps the card's trip-chain position with one of their OWN chain entries — a play, a board placeholder, or a rooted card — `actions.mjs` `showSwapPopup`); it is **optional** (omitted when the player plays without a swap) and ignored for every other action.
- **Polling** (`GET /api/state/{game}/{me}`, every 2.5 s) exists because the WS only *receives* a state when **we** send — polling is how we see the **opponent's** actions. Both feed the same `applyState()` funnel.
- `applyState()` is the single entry to rendering; there is no other path that mutates `game.state` or the DOM game view.

`applyState(st)` (single funnel for WS replies **and** poll responses): **snapshots
the old DOM zones for the card-movement animation** (see `anim.mjs`), resets
cell-select, stores state, `renderAll()` (in `state.mjs`), **flies the
zone-transition cards** (hand→stopover, stopover→discard, deck→hand, …), shows
server message, endgame detection.

`enterGame()` (in `game.mjs`): switch view, connect WS, start polling.

## Action dispatch (what a play sends, per phase) — `actions.mjs` `dispatchPlay`

`dispatchPlay(cardId, col=null)` is the **single** play path used by both the
`#btn-play` button and a click/drop on a stopover slot. It handles the engineer
cases (drop → cell select, refinery → dwelling zone), the doctor cases
(pending → pending zone, laboratory → dwelling zone), the mage cases
(Celestial_reversal → day/night fix popup, thermic_flux → +4/−4 °C popup,
Apocalypticritual → cataclysm-order popup, **black_hole → dwelling zone** — it is
a **dwelling** card, placed like the refinery/laboratory), and
the normal stopover case (with the pending card selection popup when applicable).
The `black_hole` **TAP** (not the placement) is handled by the `#btn-tap` handler in
`zones.mjs` (a **CW / CCW rotation popup** — see the *Tap dwelling* row).
Branch order: engineer drop →
engineer dwelling → doctor pending → doctor dwelling → **Mage black_hole (dwelling)**
→ **Mage Celestial_reversal** → **Mage thermic_flux** → **Mage Apocalypticritual**
→ normal move card:

| Phase | Selection | Message |
|---|---|---|
| `init-mana` | up to 3 (sent **one by one**) | `{cards:[id], to:"mana", mode:""}` |
| `mana-pass` | exactly 1 | `{cards:[id], to:"mana", mode:""}` |
| `play` + engineer **drop** | 1 → `enterCellSelect` → cell click | `{cards:[id], to:"stopover_N", mode:"move", cell:<0-23>}` |
| `play` + **refinery** (dwelling) | 1 | `{cards:[id], to:"dwelling", mode:""}` (slot must be empty) |
| `play` + doctor **pending** (epo/virus/bloodtest/mercurochrome) | 1 | `{cards:[id], to:"pending_zone", mode:""}` |
| `play` + **laboratory** (dwelling) | 1 | `{cards:[id], to:"dwelling", mode:""}` (slot must be empty) |
| `play` + **Celestial_reversal** (Mages) | 1 → `showDayNightPopup` → Day/Night click | `{cards:[id], to:"stopover_N", mode:"move", day_night:"day"|"night"}` — the popup asks the player to fix day or night for the rest of the game; the chosen value is sent as `day_night`. The card is an **INSTANT no-op** on the trip chain (advancing 0, like a placeholder) — the day/night fix fires at play time and cannot be blocked/canceled/conditioned. |
| `play` + **nobodymoves** (Mages) | 1 (no popup) | `{cards:[id], to:"stopover_N", mode:"move"}` — played in **MOVE mode** like a normal card (no popup, no special dispatch). The card is an **INSTANT no-op** on the trip chain (advancing 0, like a placeholder) — the movement-lock effect fires at play time and cannot be blocked/canceled/conditioned. It **locks ALL players' movement** for the rest of the turn (their MOVE cards still resolve — condition + non-movement effects fire, defend/block race still happens — but no advancing / movement effects; only unstoppable may move; defend is unaffected). The lock is cleared in the cleaning phase. |
| `play` + **thermic_flux** (Mages) | 1 → `showThermicFluxPopup` → +4 °C / −4 °C click | `{cards:[id], to:"stopover_N", mode:"move", temp_change:"up"|"down"}` — the popup asks the player to change the planet temperature by +4 or −4 °C (clamped 1..20, permanent); the chosen direction is sent as `temp_change`. The card is an **INSTANT no-op** on the trip chain (advancing 0, like a placeholder) — the temperature change fires at play time and cannot be blocked/canceled/conditioned. |
| `play` + **Apocalypticritual** (Mages) | 1 → `showApocalypticritualPopup` → 4 ordered biome selectors → Confirm | `{cards:[id], to:"stopover_N", mode:"move", cataclysm_order:[b1,b2,b3,b4]}` — the popup shows **4 ordered `<select>` rows** (one per biome, pre-filled with the current pile order from `game.state.cataclysm_pile` or the default `['OC','MO','DE','JU']`); each row has a swap-on-duplicate `<select>` so the 4 choices are always a valid permutation. Confirming sends the chosen order as `cataclysm_order` — the engine then **sets the cataclysm pile to that order** (index 0 strikes next), permanently until the next ritual. The card is an **INSTANT no-op** on the trip chain (advancing 0, like a placeholder) — the pile reorder fires at play time and cannot be blocked/canceled/conditioned. |
| `play` + **black_hole** (Mages) | 1 | `{cards:[id], to:"dwelling", mode:""}` (slot must be empty) — **a DWELLING card** (like the refinery/laboratory): it is **placed** in the dwelling zone, NOT an instant-at-play card. Its effect fires on a **TAP** (see the *Tap dwelling* row), where the player chooses the rotation direction. The card is a **no-op on the trip chain** (it is a dwelling, not a move/defend play). |
| `play` + **swap_cards** | 1 → `showSwapPopup` → drag the card onto a target (or click it) → **Play without swap** to skip | `{cards:[id], to:"stopover_N", mode:"move", swap_with:<1..5>}` — **the `swap_with` field is OPTIONAL** (omitted when the player clicks **"Play without swap"**). The popup is a **stopover mirror** of the player's OWN trip chain (5 positions, showing their plays + board placeholders (doctor pending / dwelling) + rooted cards); the player **drag-and-drops** the played card (or **clicks**) onto a target entry to **exchange their positions**, and the chosen position (1..5) is sent as `swap_with`. The engine then **swaps the card's trip-chain position with that entry** at play time (the two exchange their stopover columns — resolution order, facing, blocking all apply the swapped order). The card is **not** a no-op: it still **advances by its basic value** (from its new position) and applies its condition/effect at resolution. The swap is an **INSTANT** play-time effect (cannot be blocked/canceled/conditioned). **Defend** mode of a swap_cards card is a plain no-op (no swap). If the player also has pending cards, the **pending-attach popup comes FIRST** (chained — `showPendingPopup` with an `onSend` callback that opens `showSwapPopup`), so both the pending choice and the swap target are in the **same** action message. |
| `play` + normal card | 1 | `{cards:[id], to:"stopover_N", mode:"move", pendings:[pending? or []]}` — if the player has pending cards, a **popup** appears to select one (or skip); `pendings` carries the selected pending card name (or `[]` if none) |
| **defend button** (multi-card defense) | 1–5 main cards (support cards excluded; multi-select **in the popup**) | `{cards:[…1..5…], to:"stopover_N", mode:"defend"}` — cost = **Σ of the selected cards' mana**, shield = **Σ of their shields** (the block on the opponent's card on that stopover only holds if Σ shield ≥ the opponent card's cost). **Popup flow** (`showDefendPopup`, `actions.mjs`): the `#btn-defend` click opens the modal — enabled when it's my play turn + a free position remains + I hold ≥ 1 main card (**no pre-selection in hand required**); the modal lists **all** hand cards (`.defend-cards` grid — main cards selectable, support cards greyed + unselectable), toggling 1–5 main cards (cap 5 + toast); the **live summary** shows `k / 5 selected • cost Σ • shield Σ` and the **⛨ DEFEND button at the bottom** (full-width) is enabled iff 1–5 selected and cost ≤ available mana. Backdrop click closes; a **rejected** answer (`{success:false}`) toasts and keeps the popup + selection open; on success the modal is removed (`renderAll`'s sweep — `state.mjs` — closes it as soon as the phase leaves my play turn). The defend group renders in `board.mjs` as a **diagonal fan** (each extra card peeks out up + right, z-index rising) so all engaged cards stay visible |
| pass button | — | `{cards:[], to:"", mode:"pass"}` — **mana phase: pass is always allowed** (the engine decides whose pass it is); **play phase: gated on `myTurn()`** (2026-09-04 fix: `myTurn()` is false in the "mana-pass" phase because `detectPhase` returns the string `"mana-pass"` — the old handler used `myTurn()` as a global guard, making the mana-phase pass a silent no-op and stalling the game) |
| **Tap dwelling** button | — | `{cards:[], to:"dwelling", mode:"dwelling_activation"[, rotation:"cw"\|"ccw"]}` (free, once/turn; button visible iff `me.dwelling && !me.dwelling_tapped` and my play turn; **laboratory** tap adds an `epo` pending card instead of drawing; **black_hole** tap opens a **CW / CCW rotation popup** — `zones.mjs` `#btn-tap` handler calls `showBlackHoleRotationPopup` and the chosen direction is sent as `rotation`; the engine then **rotates the earth 3 cells** in that direction — the 4 biomes shift position in `game.state.earth` while all tokens stay on their cell index, cumulative + permanent (game-level state `game.state.earth_rotation`); the background art **rotates** via CSS `transform: rotate(earth_rotation × 15deg)` on `#board-bg` (see the *Earth background* note below)) |
| **Discard popup** (discard selection) | exactly N (multi-select in the popup, N from the phase) | `{cards:[…N…], to:"discard_pile", mode:""}` — the trip chain is PAUSED mid-resolution waiting for this; only the discarding player's answer is accepted. **Popup flow** (`showDiscardPopup`, `actions.mjs`): `renderAll` **auto-opens** the modal when the phase is "waiting for **me** to discard N" (idempotent — `.discard-popup` guard; `renderAll` runs on every 2.5 s poll); the modal lists **all** hand cards (delegated 3x hover preview), toggling selects exactly N (cap + toast), and the **red DISCARD button at the bottom** is enabled iff exactly N are selected. Backdrop click closes it (the engine stays paused — a hand-card click **reopens** it; `renderAll` re-opens it on the next state too). A **rejected** answer (`{success:false}`) toasts and keeps the popup + selection open; on success the modal is removed (the state change's `renderAll` sweep double-covers). While the popup phase is active **all action-bar buttons stay hidden** (play/defend/pass/tap); the **old `#btn-discard` action-bar button and the `#dz-discard` drag-drop target are removed** (the discard panel is display-only) |

Every send is preceded by the same guards: my turn → slot order (a **free** position must remain in the player's OWN chain — `freeCols(game.state, game.me).length`; the player's rooted cards occupy the leading positions of that player's chain, so plays go AFTER them — **per-player**, never shared; column from `nextSlotCol`) → `checkMana(id)`. The stopovers are **per-player** — each player has their own 5 positions, and a player's rooted cards (on the board from the previous turn) occupy the leading positions of that player's chain. The engine is the source of truth (it overwrites the client's `to`); the frontend mirrors the rule for display + validation.

## Rendering invariants

- **Full re-render per state**: `applyState → renderAll` rewrites the zones, board rows and slots from scratch (the DOM is treated as a function of `game.state`). The exceptions are deliberately transient/incremental: the **log** (`game.logTurnsRendered` + `game.logTurnSigs`, `log.mjs` — new turns are appended, and a rendered turn whose **content changed** is re-rendered in place keeping its open/closed state; this is required because the engine creates a turn's log entry at PLAY TIME with only the `instant` section, and the same entry gains its stopovers when the trip chain resolves — the turn count does not change), the **selection/cell-select** UI, the **trip-chain animation** (its `.chain-anim-card` clones are spared by `renderBoard`'s slot wipe — `.played:not(.chain-anim-card)` — and torn down by `clearChainAnim` / the run's final step; `chainGen` invalidates pending timers, `chainanim.mjs`), the **player markers** (`.marker[data-player=…]` are repositioned across renders so their CSS left/top transition animates the advance — `board.mjs`), and the **card-flight clones** (`.card-fly`, self-removing, never part of the rendered state — `anim.mjs`).
- **Delegated hover**: card hover previews are wired on `document` (mouseover/mouseout + `.card[data-hover-id]`), not per element — required because re-renders destroy per-element listeners. New card elements **must** keep `data-hover-id`. (in `modals.mjs`)
- **Faction themes (per-faction colors, editable JSON)**: `static/faction_themes.json` holds each MAIN faction's UI palette (`accent` / `accent2` / `onAccent`, sampled from the faction logo) — **edit it and reload to change the colors, no code change**. `theme.mjs` sets three CSS variables per scope (`--accent`, `--accent-2`, `--accent-contrast`; opponent variant `--oppo-accent*`) and `style.css` derives **every** tint/glow/gradient from them with `color-mix()` (so a scope override cascades — there are **no hardcoded accent hex/rgba left** in the CSS except the `:root` default teal/orange fallback). Scopes: the **setup page** `#view-setup` (the selected faction's palette; hover previews), the **game page** `#view-game` (MY faction = page default via `syncGameThemes` in `renderAll`), the **opponent containers** `#oppo-zone` / `#oppo-side` / `#row-oppo` (THEIR faction's palette), and the opponent's **board marker** (`.marker.oppo` reads `--oppo-accent` set on `#view-game`). The page **background stays the dark blue** (`--bg-*`), and the mana-drop / tap / discard mechanic colors (purple / red) are intentionally NOT themed. `anim.mjs` `flyCard` copies the current `--accent` onto its body-level clone so the flight glow ring matches. (`theme.mjs` + `setup.mjs` + `state.mjs` + `style.css`)
- **Stopover slot watermarks**: every stopover slot contains the **row owner's main-faction logo** as a faint watermark (`.slot-logo`, built once in `buildBoard`, src set per row in `renderBoard` from `p.faction` via `factionLogoSrc`) — my row shows MY logo, the opponent's row shows THEIRS; hidden when the faction is unknown. Purely visual, behind the cards (z-index 0). (`board.mjs` + `style.css`)
- **Art fallback**: every `<img>` gets `onerror → /placeholder.svg`; support art via `cardImg()` (`card_path`), main art `<card_id>.png`. (helper `cardEl()` in `cards.mjs`)
- **Earth background = the board's biome configuration, ROTATED to match the black_hole**: `renderBoard` sets `#board-bg`'s src via `earthBgSrc(b0)` where **`b0` is the INITIAL cell-0 biome** (`game.state.earth_initial_b0`, fallback `game.state.earth[0][0]`) — pinning the base image to the **initial** order so it is stable across rotations. Then it applies a CSS **`transform: rotate(earth_rotation × 15deg)`** to `#board-bg` (the board geometry is 15° per cell — `POS24[i] = polar(-90 + (i+0.5) × 15°, 49)`), so the background artwork **rotates** (not swaps) to match the engine's biome positions after a `black_hole` tap (`game.state.earth_rotation`, the signed cumulative cell count; e.g. +3 after one CW tap → 45°). `style.css` gives `#board-bg` `transform-origin: center; transition: transform 0.6s ease` (the 0.6s transition animates the rotation on a tap). The src is only touched **when it actually changes** (the image is ~10 MB and the board re-renders on every state poll — a naive set would re-trigger the download each time); the `transform` is set every render (it is cheap and only animates when the value changes). `index.html` keeps the plain playmat as the pre-game/default src. (the rotation is verified visually via a before/after browser screenshot — the background art aligns with the biome segments after a tap). (`board.mjs` + `cards.mjs` + `style.css`)
- **Board tokens**: positions come from `POS24` (polar ring, radius 49 %, cell 0 at top, clockwise); stacks of trap/drop tokens fan up-right (`MAX_FAN`, `FAN_STEP_PX`) with a count badge on the top one. Drop tokens are **cleared and redrawn from state on every render** (`.drop-token` elements are NOT persisted — unlike the player `.marker`s): `renderBoard` removes the old ones before redrawing, so a token the engine **consumed** (a player stepped on it / jump-landed on it) disappears on the next poll, and polls don't stack duplicate images. (`board.mjs`)
- **Persistent board cards (survivors of the cleaning phase)**: three game-level / per-player fields render **outside** the `action_chain` into the stopover slots — the **dwelling placeholder** (`p.dwelling_slot`, a faction placeholder image in the slot the refinery/laboratory would have occupied, cleared by the engine in the cleaning phase), **pending placeholders** (`p.pending_slots`, the **per-turn placeholder list** — cleared by the engine in the cleaning phase, so it is **NOT parallel** to the persistent `p.pendings` zone; rendered with the **same faction-specific placeholder image as the dwelling cards** — `dwellingPlaceholderSrc(p.faction)` — with a gold border. Entry shape: **`[card, slot]` pair** (the laboratory TAP's `epo` = a `null` slot). The render loop and `playedCount()` normalize the shape and skip `null` slots. **the placeholder of a pending card attached the SAME turn STAYS IN PLACE** (the engine keeps the `[card, slot]` pair in `pending_slots` until the cleaning phase — it marks the consumed trip-chain position, so the next play lands one position further and `playedCount()` — which counts the non-null entries — stays consistent with the engine's `play_count`; the tooltip reads "attached to a card this turn" when the pair's card is an action's `pending_card`), and **rooted-on-board cards** (game-level `rooted_on_board: [{card_id, owner, stopover}]` — a card that earned a rooted token last turn survives on a stopover until the end of the NEXT turn). All three are rendered per owner row in `renderBoard` (stale elements are always removed first — the slots are built once, not rebuilt), all count as a **filled** slot, and none are part of the `anim.mjs` diff (they appear in place, no flight — same as the placeholder). The rooted card gets the green "stuck to the trip chain" frame + the rooted-token chip (`.rooted-on-board`, `.rooted-token` in `style.css`: a **circular dark-disc chip with the tree-with-roots icon + green ring**, top-right corner — a bare transparent PNG at the corner used to blend into the card art) and a `🌱 rooted — …` tooltip. **(per-player stopovers)**: the rooted card is a **normal card** on the trip chain — it occupies the **leading position** of its owner's chain (position 1, 2, …) and applies its **basic advancing** when the chain resolves (no condition, no effect, but **BLOCKABLE** by the opponent's defend at the same per-player position). The owner's plays go **AFTER** the rooted cards (position R+1, …), so the rooted card **consumes a slot** and the opponent's cards are independent (per-player positions never shift each other). **Per-player rule (the main path)**: a player's next position = `rootedCount(owner) + playedCount(owner) + 1`; column = `5 − position`. The rule is **per-player** (each player has their OWN 5 positions), so the two players' cards may legitimately share a column (e.g. both at `stopover_4`). **Single source of truth: `ge._player_stopover(game, name, played_before)`** in `engine/game_engine.py` (with `ge._player_chain`) — the engine uses it for its own placements (dwelling placeholder slot + rooted end-of-turn placement) and the robot calls it directly (`ai_driver.py`); the **frontend mirrors it in JS** in `actions.mjs` (`rootedCount`/`playedCount`/`nextPosition`/`freeCols`/`nextSlotCol` — it cannot import Python; the mirror was verified case-by-case against the engine). `canDropOnStopover` guards on `freeCols(st, me)`, the defend/Pass-area guards and the engineer-drop `confirmCell` guard do the same, and `orderHint` names the player's next position. **Layered slot (defensive fallback)**: a play and a rooted card can still share a column in old games (the shared-stopover model) — in that case `renderBoard` tags the rooted card `.behind` (smaller, nudged up, tilted, z-index 1 under the full-size played card) so the slot reads "your card on top, the last-turn survivor underneath" — and **re-parents the token chip to the slot** (class `.chip-float`, z-index 6) so it escapes the rooted card's low z-index and stays clearly on top of the played card (top-right corner); the render-loop cleanup removes both `.rooted-on-board` elements **and** any orphaned `.rooted-token` in the slot before re-adding everything. (`board.mjs` + `style.css`)
- **Player tokens show the faction logo**: each player's Earth token (`.marker`) renders the player's **faction logo** (`.marker-logo`, `factionLogoSrc(p.faction)` → `/assets/logo_<key>.png`) filling the circle, with the player's **first letter** on top (`.marker-letter`); the me/oppo gradient doubles as the fallback background when the logo is missing (unknown faction → letter-only circle). The marker element is persisted across renders (repositioned, not recreated — see above), so the inner HTML (logo + letter) is rebuilt on every render without affecting the left/top slide transition. (`board.mjs` + `style.css`)
- **Opponent masking**: never render opponent hand/mana/deck *contents* — counts (`*_count` or list length) and card backs only. (`zones.mjs`)
- **Mana-waiting glow**: `#dz-mana` gets the `.mana-waiting` class (a dark↔light-purple `drop-shadow` pulse, `@keyframes manaPulse` in `style.css`) **only** while the phase is `init-mana` or `mana-pass` — toggled in `renderMyZone` from the same `canMana` gate that enables the drag target, so it lights up exactly when a card may be dropped into the mana zone and is removed on every other phase (re-renders are idempotent: `classList.toggle(..., canMana)`). Purely visual. (`zones.mjs` + `style.css`)

### Card-movement animation (`anim.mjs`)

Because `renderAll` rewrites the DOM from state, cards would otherwise pop between
zones; this layer diffs the **previous** and **new** state per player and flies a
lightweight clone (`.card-fly`) from the old zone rect to the new one. Functions (in
order): `snapshotZones()` (captures the CURRENT hand-card rects + occupied stopover
rects **before** `renderAll` — called from `applyState`), `planPlayerMoves(oldP, newP,
isMe, player)` (set-diff of the public `action_chain` → hand→stopover (new plays) &
stopover→discard (cleaning / mid-chain-win flush); dwelling place/wreck; my hand
diffed by **id** (drawn / played / discarded / to-mana), the opponent's by **counts**
(card backs) since the hand is masked; a mana increase with no matching hand loss =
ramp (deck→mana), a mana decrease = taxation (mana→discard); a discard decrease + deck
increase = reshuffle), `sourceRect` / `destRect` (resolve a move to concrete rects —
live DOM for destinations, the pre-render snapshot for sources; **ID GOTCHA: the
stopover rows are `#row-me` / `#row-oppo` and `slotEls.me` / `slotEls.oppo`, while the
zone panels are `#my-*` / `#oppo-*`** — the two row ids differ), `flyCard(fromRect,
toRect, cardId, delay)` (fixed-position clone, 2×rAF then a CSS transform/opacity
transition; `FLY_MS=520ms`, stagger `FLY_STAGGER_MS=110ms`; removed after; face-down
variant `.card-fly.back` for the opponent), `flipMyHand(snap)` (FLIP: the remaining
hand cards slide to their re-centered position instead of jumping),
`popNewBoardTokens` (new pet-trap / engineer drop tokens get the `.token-pop`
animation), `animateZoneTransitions(oldSt, newSt, snap)` (orchestrates: my flights
first, priority order plays → zone shuffling → draws). Constants: `FLY_MS`,
`FLY_STAGGER_MS`, `REDUCED_MOTION` (skip all flights under `prefers-reduced-motion`).
Purely visual — no state, no messages, no engine coupling. **Known approximation**:
when several engine actions land between two observed states (fast polling gaps), the
diff attributes the net change (e.g. a mana increase with no matching hand loss flies
deck→mana instead of hand→mana); the zone counts always end up correct because they
come from the state, only the flight's origin may be a generic back.

### Trip-chain resolution animation (`chainanim.mjs`)

`playChainAnim(t)`, `placeThread(col, firstRow)`, `clearChainAnim()` + `chainGen`
(generation counter). Triggered from `renderLog` when a **genuinely new** turn log
entry appears (`added && start > 0` — never on the first render/rejoin): the stopover
slots pulse in the **exact resolution order** read from the public turn log (the
engine appends the log's stopovers in resolution order and each entry carries `order`
1/2 = first/second player of the turn): stopover by stopover, first player's card
(gold, `chain-first`) then second player's (silver, `chain-second`), ~460 ms per card.
A glowing thread (`.chain-thread`, gold end on the first player's side via
`first-top`/`first-bottom`) bridges the two facing slots while a stopover has both
players' cards. Because the cleaning phase already cleared `action_chain` in the new
state, the played cards are **cloned** onto the slots for the duration of the run
(`.chain-anim-card`, keeps `.played` for layout + `data-hover-id` for the delegated
hover preview) and removed when the run ends. `clearChainAnim()` bumps `chainGen` so
pending `setTimeout(step)` calls are dead on arrival, and removes all pulse classes /
threads / clones. `renderBoard`'s slot wipe uses `.played:not(.chain-anim-card)` so
clones survive mid-run polling re-renders. Purely visual — no state, no messages, no
engine coupling.

## Discard-pile viewer (click a discard side-panel → popup with all discarded cards)

- **Trigger**: clicking either discard side-panel (`#my-side .side-panel.discard` or `#oppo-side .side-panel.discard`) opens a **read-only** modal (`.discard-pile-popup`) listing **all** of that player's discarded cards (most recent first) with the standard 3x hover preview (`cardEl()` → `data-hover-id` → the delegated `modals.mjs` hover). The panels are static DOM, so the listeners (wired at the top level of `modals.mjs`) survive re-renders; both panels gain `cursor:pointer` + a "click to see all discarded cards" title hint.
- **Both piles are public** — the engine masks only hand/mana/deck (`current_game_json`), so `st.players[name].discard` is the full id list for both players; no new endpoint, no message sent.
- **Empty pile** → no modal, a toast (`"<name>: the discard pile is empty"`).
- **Always fresh**: `renderAll` (`state.mjs`) calls `refreshDiscardPilePopup()` (no-op when closed) on every state, so an open popup rebuilds in place as cards are discarded — it never shows a stale pile.
- **Closing**: `✕ Close` button or backdrop click; `leaveGame` removes it.
- CSS: `.modal.discard-pile-popup` in `style.css` (760px box, scrollable `minmax(84px, 1fr)` grid, `max-height:55vh`).

## Checklist when modifying the frontend

1. New engine rule / message field (e.g. a new `to:` target, a new board field) → mirror it in: `sendAction` call site (`comm.mjs`), `renderAll`/zone renderers (`state.mjs`/`zones.mjs`/`board.mjs`), **and** the "mirrors the engine" table above.
2. New card type / support faction → extend `cardInfo`/`cardTitle`/`cardImg`/`cardCost` (`cards.mjs`), the UI constants (`ENGINEER_DROPS` pattern) and the button dispatch table (`actions.mjs`).
3. Any change to `renderLog` / `applyState` / `makeHandCard` → keep the incremental + delegation invariants (section "Rendering invariants").
4. New module or new top-level cross-module `const` read → re-verify the import graph is still TDZ-safe (see "Import graph").
5. Update this file in the same commit.

## Change log

- **2026-09-24 — Condition/effect legend in the in-game card hover preview.** The 3x hover preview (bottom-left, `#card-hover`) now shows the card's **condition and effect as readable text under the image**: `modals.mjs` gained `COND_TOOLTIPS` + `EFF_TOOLTIPS` (one line per pool `condition`/`effect` value, provided by the designer) and `hoverCardText(id)` — `showCardHover` fills the new `#card-hover-text` div (in `index.html`, inside `#card-hover`) with a **Condition** line + an **Effect** line. The effect line's `"X"` is the card's `effect_number` shown as its **magnitude** (`Math.abs` — the pool stores the direction signed: `backward`/`discard` are negative, and the legend line already says the direction: "Move backward by 1 cells, clamped at cell 0"). **Support cards** have no condition/effect — they show their pool `description` under the Effect label (no Condition line). **Card backs / unknown ids / pool not loaded** → the legend is hidden (`.show` removed), image-only as before. `style.css`: `#card-hover` lost its fixed `aspect-ratio` (height is now image + legend), the `img` carries the `180/260` aspect-ratio instead, and `#card-hover-text` styles the legend panel (dark backdrop, `--accent-2` uppercase labels — `.ch-row`/`.ch-label`). Purely presentational — no engine/message/AI change; the zoom modal (`showCardModal`) is untouched. Verified in-browser (AI game, Dwarves+Engineers): main cards show condition + effect with the number (e.g. "This condition is a trigger. The top card of the cataclysm pile … | Move forward by 3 cells"), support cards show their description (landmine/gluetrap/boost), card back hides the legend. Test game `26_09_24_00_24_26_HUkXH` deleted afterwards. `index.html` cache-bust bumped `?v=47` → `?v=48`.
- **2026-09-23 — Faction tooltips on the setup page (main + support).** Every faction button in the setup page (the 6 main factions + the 3 support factions) now shows a **hover tooltip with a short role description** — mana curve, block power, signature effect (text from the design slides: Dwarves = high shield/high curve/blockers + Avalanche; Demons = control & attrition/low shield + Effect cancelled; Twigs = speed through generosity + Rooted; Miaous = all condition/effect combos but little raw advancing + Pet trap; Orcs = speed through sacrifice, Unstoppable + Swap Cards; Mummies = exploit the opponent's state, Copy effect; Engineers = drops on the map + refinery; Doctors = pending zone + laboratory; Mages = temperature/day-night/cataclysm/earth rotation). The **tooltip background follows the faction's own color**: main factions read `faction_themes.json` via `factionTheme(name)` (re-painted once the async JSON lands — `setup.mjs` `addFactionTip`, `paint()` after `loadFactionThemes()`), the 3 support factions use their **banner colors** (`SUPPORT_TIP_COLORS`: Engineers brown `#a36244`, Mages blue `#3d9df0`, Doctors red `#e8555f` — sampled from the banners, no `faction_themes.json` entry for supports). The tip text color = the faction's `onAccent` (or a light tint for the supports). Implementation: `setup.mjs` gained `FACTION_TIPS` (name → text), `SUPPORT_TIP_COLORS`, and `addFactionTip(btn, name, supportKey?)` (called from `renderFactions` / `renderSupportFactions`; the tip is a `<span class="faction-tip">` inside the button, `--tip-bg` + `color` set inline); `style.css` gained `.faction-tip` (absolute above the button, arrow `::after`, `display:none` → shown by `.faction-btn:hover` / `.supfac-btn:hover`, `pointer-events:none`) and `position:relative` on the two button classes. Purely presentational — no engine/message/AI change, and the existing hover palette-preview (`onmouseenter`/`setSetupTheme`) is untouched. Verified in-browser: all 9 tips present, hidden by default, correct per-faction backgrounds (Dwarves `#459fe0` … Doctors `#e8555f`), correct contrast text. `index.html` cache-bust bumped `?v=46` → `?v=47`.
- **2026-09-22 — Per-faction theme colors (setup page + in-game) + stopover slot logos.** The UI now themes itself with the player's MAIN faction colors (sampled from the faction logos), configured in a new editable file **`static/faction_themes.json`** (per-faction `accent` / `accent2` / `onAccent` — edit + reload, no code change; served no-cache like the background-card params). New module **`theme.mjs`** (`loadFactionThemes` / `factionTheme` / `applyFactionTheme` / `applyOpponentTheme` / `syncGameThemes` / `THEME_DEFAULTS`): it sets only three CSS variables per scope (`--accent`, `--accent-2`, `--accent-contrast`; opponent variant `--oppo-*`) and `style.css` was refactored so **all** accent tints/glows/gradients derive from them via `color-mix()` (the 29 hardcoded `rgba(79,209,197,…)` / `rgba(246,173,85,…)` values are gone — only the `:root` teal/orange defaults remain as fallback). Scopes: the **setup page** (`#view-setup` — picking a faction recolors headings, tabs, buttons, selected-faction frame, AND the **panel containers' background + border** — **hovering a faction button previews its palette**, mouseleave restores the selection; **loading a text-file deck also themes the page** with the loaded deck's dominant faction (majority of the cards) — `setup.mjs` `setSetupTheme`), the **game page** (`#view-game` gets MY faction via `syncGameThemes` at the top of `renderAll` — `state.mjs`), the **opponent's containers** (`#oppo-zone` / `#oppo-side` / `#row-oppo` get the OPPONENT's faction — so both players' banners, side rows, counters and stopover rows fit their own main faction), and the **board markers** (`.marker.me` reads `--accent`, `.marker.oppo` reads `--oppo-accent` — each token shows its own faction color). **Container tint (2026-09-22 follow-up)**: the *containers* themselves — setup page panels (`.panel`), the dwelling / pending / deck / discard boxes (`.side-panel`), and the hand band (`.big-zone`) — now have their **background AND border** mixed from the scoped accent with `color-mix()` (a very dark shade of the main faction: my containers in MY color, the opponent's containers in THEIR color, same scoping — no extra JS needed). The page **background stays the dark blue** (`--bg-*` untouched) and the mana/tap/discard mechanic colors (purple / red) are intentionally not themed. **Stopover slots now show the row owner's faction logo as a watermark** (`.slot-logo` — built in `board.mjs` `buildBoard`, src set per row in `renderBoard` from `p.faction`; faint, behind the cards, hidden when the faction is unknown). `anim.mjs` `flyCard` copies the current `--accent` onto its body-level flight clone so the glow ring matches. `theme.mjs` defers the application until the JSON is loaded (last request per element+role wins, `WeakMap`), so an early click (faction pick right after page load) still themes correctly. **Unchanged**: engine, message shapes, AI, replay, all rules. Verified in-browser (Orcs player vs Dwarves Robot): setup page red, in-game my row/buttons red + Orcs watermark in my slots, opponent row/blue + Dwarves watermark in theirs, both markers faction-colored, background unchanged. `index.html` cache-bust bumped `?v=42` → `?v=43`.

- **2026-09-22 — Discard-pile viewer (click a discard side-panel → popup with all discarded cards).** Clicking either discard side-panel (my row `#my-side` or the opponent's row `#oppo-side`) opens a **read-only** modal (`.discard-pile-popup`, `modals.mjs` **`showDiscardPilePopup(name)`**) listing **all** of that player's discarded cards (most recent first) with the standard 3x hover preview (cards built with `cardEl()` → `data-hover-id` → the delegated `modals.mjs` hover). No new endpoint / no message: **both discard piles are public** — `current_game_json` masks only hand/mana/deck, so `st.players[name].discard` is the full id list for both players. The panels are static DOM, so the click listeners (top-level wiring in `modals.mjs`) survive `renderAll` re-renders; both gain `cursor:pointer` + a "click to see all discarded cards" title hint. Empty pile → no modal, a toast. `renderAll` (`state.mjs`) calls **`refreshDiscardPilePopup()`** (no-op when closed) on every state, so an open popup rebuilds in place and never shows a stale pile. Closing: `✕ Close` button or backdrop; `leaveGame` also removes it. CSS: `.modal.discard-pile-popup` in `style.css` (760px box, scrollable `minmax(84px, 1fr)` grid, `max-height:55vh`). **Unchanged**: engine, message shapes, AI. Verified in-browser (vs-AI game): opponent's pile popup listed all 5 discarded cards (and grew 2→5 in place across polls), my panel's empty case toasted, and after playing a card my panel's popup listed it. `index.html` cache-bust bumped `?v=40`/`?v=41` → `?v=41`/`?v=42` (css + app.mjs).
- **2026-09-22 — Hover on the setup-page cards (deck previews + background).** The **deck-preview cards** (main 20 + support 10) now get the game-page **3x hover preview**: their render loops (`renderDeckPreview` / `renderSupportPreview`) use the shared `cardEl()` from `cards.mjs` (sets `dataset.hoverId`, title, art + fallback) instead of hand-rolled markup — the delegated `modals.mjs` hover then picks them up (selector `.card, .bg-card`, `dataset.hoverId` required). The **background cards** work the same way: hovering a floating card **freezes it in place** (its `:hover` state gates the motion in `bg.mjs` `tick()`, so it doesn't drift away from the cursor) and the **3x preview** appears (bottom-left `#card-hover`) — the picker keeps each card's **`card_id`** (`pickCardId()` → `el.dataset.hoverId`), so main and support cards both resolve through the existing `cardImg()`. For the mouse to reach the cards behind the setup panel: `#view-setup { pointer-events: none }` with `#view-setup button, input, label, .card { pointer-events: auto }` (all interactive elements stay clickable — verified), and `.bg-card` gained `pointer-events:auto; cursor: zoom-in` + a `:hover` brightness/saturation lift (style.css). **Unchanged**: engine, message shapes, AI, game-page behavior (the deck-preview cards keep their small hover lift too).
- **2026-09-22 — Animated card background on the setup page (config-driven).** New module `bg.mjs` + new file `static/background_cards_parameters.json` (served at `/static/background_cards_parameters.json`, no-cache — **edit the JSON and reload to explore configurations, no code change**). The setup page gets a full-viewport `#bg-cards` layer (created lazily, `position:fixed; z-index:0; pointer-events:none`) that spawns random card arts with a **fixed equal-faction distribution** — pick one of **9 equal buckets** (6 main factions + 3 support factions → each faction exactly **1/9** of the cards, i.e. 2/3 main split 1/6 each + 1/3 support split 1/3 each; buckets built once from `CARDPOOL`/`SUPPORT` via `FACTIONS`/`SUPPORT_FACS`) — floating **bottom → top** at low opacity, rotating, fading in at the bottom edge and out at the top; the setup content stays on top (`#view-setup` gained `position:relative; z-index:1` in `style.css`). The JSON knobs: `enabled`, `cards_per_minute` (spawn rate), `speed_px_per_s` (rise speed, per-card ±40 % jitter), `card_width_px` + `size_jitter` (size, ± % width variation), `rotation_speed_deg_per_s` (per-card random CW/CCW direction), `rotation_range_deg` [min,max] (starting rotation in degrees — 0 = straight vertical, 90 = tapped sideways; e.g. `[0,0]` for none), `opacity` (0..1, default 0.3 per spec), `spread_px` (horizontal reach from the **screen center**: the card center appears within ± `spread_px` of the page middle — less = narrow band around the center, more = wider space; a value ≥ half the screen width = full width, default `9999` = full width), `max_cards` (on-screen cap). Implementation (2026-09-22): `.bg-card` gained `overflow:hidden` (rounded 9px corners, images clipped) and the spawn x-range is clamped fully on-screen; the horizontal spawn is the card **center** uniform within ± `spread_px` of the screen center (clamped on-screen). Earlier: a single `requestAnimationFrame` loop (dt-clamped for tab switches) updates `transform: translate3d + rotate` + edge-fade `opacity` per card and recycles elements off the top; on start, ~max_cards/4 cards are **queued just below the bottom edge** (staggered) so they all **enter from the bottom** and flow up one after another (never pre-placed mid-screen); the transform + opacity are written **at spawn time** (`applyCard`), not only in the rAF loop, so there is never a flash of unstyled cards stacked top-left before the first tick (or while rAF is paused in a background tab). **Wiring**: `setup.mjs` `boot()` calls `startBgCards()` after the card pools load (art via the existing `cardImg()` + placeholder `onerror` fallback); `game.mjs` `enterGame()` calls `stopBgCards()` (cancels the rAF and removes the layer — the background is **setup-page only**); `modals.mjs` `leaveGame()` (both the endgame "Back to home" and the "✕ Leave" paths — the page is **not** reloaded) calls `startBgCards()` to restart the background when returning home (added 2026-09-22 after the "no cards on the home page after a game" bug). Config fetch failure or `enabled:false` → no background (a toast on read failure). Verified in the browser: cards visible behind the setup panels, rising + rotating, and an extreme-configuration edit (rotation `[0,0]`, opacity 0.9, 200 px/s) was applied on plain page reload. **Unchanged**: engine, message shapes, AI, game-page rendering. `index.html` cache-bust bumped `?v=35` → `?v=36` (css + app.mjs).
- **2026-09-21 — the dwelling placeholder STAYS IN PLACE after a wrecking_ball (engine rule + frontend gate).** Engine (the actual bug — game `26_09_20_18_54_55_plQOZ` turn 2): when the Robot's wrecking_ball removed the black_hole dwelling, the engine cleared `dwelling_slot` along with `dwelling` — the placeholder VANISHED while the consumed trip-chain position stayed consumed (`play_count` untouched), so the slot was left empty (a "hole") and the UI's next-slot hint disagreed with the engine. Designer decision: the placeholder must **stay in place** — only the dwelling card goes to the discard. Engine: the wreck branch (`_apply_instant_effects`) no longer clears `oppo.dwelling_slot`; `play_count` is untouched in all versions (the position stays consumed — the owner's next play lands on the position AFTER the placeholder). `ge._player_chain` already includes the placeholder from `dwelling_slot` alone, so the chain is unchanged. Replay: the wreck mirror keeps the placeholder in place (matching the engine). Frontend: `board.mjs` renders the `.dwelling-placeholder` when `p.dwelling_slot !== null` (was `p.dwelling && …`) and the tooltip adapts to the missing card ("the card was wrecked this turn; the slot stays occupied"); `actions.mjs` `playedCount()` + `occupiedCols()` gate on `p.dwelling_slot != null` (was `p.dwelling && …`) — so the placeholder still counts as a consumed position after the wreck (matching the engine's `play_count`). No changes to the message shape, the AI (the robot never owns a dwelling), or the cleaning phase (it still clears the slot at turn end — "stays in place" = until the end of the turn, like every placeholder). `index.html` cache-bust bumped `?v=33` → `?v=34`.
- **2026-09-21 — BUG FIX: a turn with an instant effect lost its trip-chain resolution in the log.** `renderLog` (`log.mjs`) was strictly incremental (append only): the engine creates a turn's log entry at PLAY TIME (an instant effect records the `instant` section, `stopovers` still empty), so a 2.5 s poll landing in that play→resolve window rendered the turn element with **only the instant section**; when the trip chain then resolved, the **same** entry gained its stopovers (the turn count did not change) and the append-only logic never updated the element — the user saw either the instant **or** the resolution, never both (turns with no instant were fine, since their entry is created only when the chain runs). Fix: `renderLog` now keeps a per-turn content signature (`game.logTurnSigs`, JSON of the turn object; reset with `logTurnsRendered` on game entry/leave) and **re-renders any rendered turn whose content changed**, in place, preserving the user's open/closed state; the trip-chain animation (`playChainAnim`) also fires when the latest turn gains its stopovers (the real resolution moment). New test: `tests/_log_render_test.html` + `tests/logrender_stub.mjs` (drives the real `renderLog` through the exact bug scenario via an import-map stub of its module deps — 14 checks; verified to FAIL with the update step disabled and pass with it). `index.html` cache-bust bumped `?v=32` → `?v=33`.

- **2026-09-21 — `swap_cards` effect — the last unimplemented effect.** The **`swap_cards`** effect (172 cards in the pool, a **MAIN-card** effect — not a support card) is now implemented as an **INSTANT chain effect**: playing it in **MOVE mode** lets the player **swap the card's trip-chain position** with one of their OWN chain entries (a play, a board placeholder (doctor pending / dwelling), or a rooted card). The two entries **exchange their stopover columns** at play time, so the trip chain resolves the **swapped order** (resolution order, facing, blocking all read the recorded columns). `swap_with` is **OPTIONAL** — without it the card simply advances as usual (the robot never sends it, so no AI change). A **defend** play is a plain no-op (no swap). The card is **not** a no-op on the chain: it still advances by its basic value (from its new position) and applies its condition/effect at resolution. Frontend changes: `cards.mjs` gained `isSwapCards(id)` (a guard — a main-card effect, NOT in `isSupportPlay`, so it is **not** excluded from the defend button); `comm.mjs` `sendAction` gained an optional **10th param `swapWith`** (an int 1..5, sent as `msg.swap_with`, omitted when the player plays without a swap); `actions.mjs` `dispatchPlay` gained a **swap_cards** branch (after the Mages branches, before the normal move fallback) that opens a new **`showSwapPopup`** — a **stopover mirror** of the player's OWN trip chain (5 positions: their plays + board placeholders + rooted cards) where the player **drag-and-drops** the played card onto a target (or **clicks** it) to exchange places, or clicks **"Play without swap"** to skip; `showPendingPopup` gained an optional `onSend` callback so the **pending-attach popup chains into the swap popup** (both choices in one action message); `log.mjs` `effectLabel` gained a `swap_cards` case; `style.css` gained `.swap-grid` / `.swap-cell` / `.swap-own` / `.swap-target` / `.swap-card` / `.swap-pos` / `.dragover` styles. **No board/zone render changes** (the swap is a trip-chain position exchange — the engine rewrites the recorded `to`/slots, and the existing stopover render already reads those). `index.html` cache-bust bumped `?v=34` → `?v=35`.

- **2026-09-21 — INSTANT section at the top of every turn in the log.** The engine now records every **instant effect** (pet_trap, engineer drops, wrecking_ball, the Mages' `Celestial_reversal` / `nobodymoves` / `thermic_flux` / `Apocalypticritual`) **and every dwelling tap** (refinery draw, laboratory `epo`, black_hole rotation) in a new **turn-level `instant` section** of the turn log — `GameState.log[turn] = {turn, instant: [{player, what}], stopovers: [...]}`. Each item is a short public phrase describing what happened (e.g. `🔮 Celestial_reversal — day/night fixed to night for the rest of the game`). The notes **moved** out of the per-card `notes` arrays (they fire at PLAY TIME or on a TAP, i.e. before / outside the trip chain, so they never belonged on a stopover line); resolution-time notes (cataclysm strikes, knockback suppression, grappling copies, …) stay in `notes`. Old games have no `instant` key — the renderer and the replay handle its absence (the replay's Apocalypticritual pile reconstruction now scans `instant` first, then the legacy `entries[].notes`; the ritual phrase keeps its `☄️ Apocalypticritual — cataclysm order set:` prefix so both formats parse). Frontend: `log.mjs` gained `buildInstantEl(items)` — `buildLogTurnEl` renders it at the **TOP** of the turn, before the stopover list (a gold-accent box, `.log-instant-sec` / `.log-instant-row` / `.log-instant-player` / `.log-instant-what` in `style.css`). **Unchanged**: the message shape, the engine's rules, the AI, and the trip-chain rendering. `index.html` cache-bust bumped `?v=31` → `?v=32`.

- **2026-09-21 — Multi-card DEFENSE moved to a POPUP (1–5 cards at once, live cost/shield totals).** The defend flow used to require pre-selecting **exactly one** hand card and then clicking `#btn-defend` (single-card only, even though the engine already supports defending **1–5 cards** in one action — cost = Σ mana, shield = Σ shields, block holds iff Σ shield ≥ the opponent card's cost). Now: clicking `#btn-defend` opens `showDefendPopup()` (`actions.mjs`, new export — same convention as `showDiscardPopup`) — a `.modal.pending-popup.defend-popup` listing **all** hand cards (`.defend-cards` grid; **main cards selectable**, **support cards greyed + unselectable** since they have no shield / can't defend), toggling **1–5** main cards (cap 5 + toast), a **live summary** (`k / 5 selected • cost Σ • shield Σ`) and the **⛨ DEFEND button at the bottom** (full-width, green defend style) enabled iff 1–5 selected and total cost ≤ available mana. The selection is a **local Set** (not `game.selected`, so it never interferes with move/mana selection). The confirm sends `{cards:[…1..5…], to:"stopover_N", mode:"defend"}` (engine unchanged — it already sums costs/shields). Backdrop click closes; a **rejected** answer (`{success:false}`) toasts and keeps the popup + selection open; on success the modal is removed. **Removed** (old single-card system): the `game.selected.size !== 1` pre-selection requirement in the `#btn-defend` handler (`zones.mjs` — now just opens the popup) and the `canDefend = … && !!selCard && !isSupportPlay(selCard)` gate in `state.mjs` (now: my play turn + a free position + ≥ 1 main card in hand). `state.mjs` `renderAll` **sweeps** `.defend-popup` whenever the phase is not my play turn (poll-renders close a stale popup). **Board**: `board.mjs` now fans a multi-card defend group **diagonally** (each extra card peeks up + right, z-index rising) so all engaged cards stay visible (the old pure-vertical offset hid landscape defend cards). **New CSS**: `.modal.defend-popup .modal-box` (760 px), `.defend-cards` grid, `.card.defend-disabled`, `.defend-summary` (+ `.defend-cost`/`.defend-shield`), `.defend-popup .defend-confirm`. **Unchanged**: the message shape, the engine (multi-defend cost/shield sums were already implemented), the AI, and the replay. `index.html` `#btn-defend` title updated + cache-bust bumped `?v=28` → `?v=29`.

- **2026-09-21 — Discard selection moved to a POPUP (red DISCARD button at the bottom of the modal).** The user disliked the old flow (multi-select hand cards + a red `#btn-discard` in the action bar) — it is replaced by a dedicated popup (same convention as the day/night, thermic_flux, Apocalypticritual and black_hole choice popups): when the engine pauses the trip chain on "turn N - waiting for NAME to discard K card(s)" and NAME is me, `renderAll` (`state.mjs`) **auto-opens** `showDiscardPopup(n)` (`actions.mjs`, new export) — a `.modal.pending-popup.discard-popup` listing **all** hand cards (`.discard-cards` grid, delegated hover preview via `data-hover-id`), local multi-select capped at N (counter `"k / N selected"`, toast on overflow), and the **red DISCARD button at the bottom** (`width:100%`, reusing the `.discard-btn` red style) enabled iff exactly N are selected. The selection is a **local Set** (not `game.selected`), so it never interferes with play/mana-pass selection. Backdrop click closes the popup (engine stays paused; a hand-card click **reopens** it — `interaction.mjs` `makeHandCard`'s click handler now opens the popup in discard phase instead of the old multi-select branch; `renderAll` also re-opens/sweeps it on every poll — idempotent via the `.discard-popup` querySelector guard). A rejected answer (`{success:false}` — wrong count / not in hand / not my turn) **toasts and keeps the popup + selection open**; on success the modal is removed (the state transition's `renderAll` sweep double-covers). While the phase is active **all action-bar buttons stay hidden**. **Removed**: the `#btn-discard` button (`index.html` + its `zones.mjs` handler), the `#dz-discard` drag-drop target (`zones.mjs` drag loop now wires only `#dz-mana`; the discard panel stays as a **display-only** pile view), the `makeHandCard` multi-select branch, and the `.side-panel.discard.over/.invalid` CSS (drop-target feedback). **New CSS**: `.modal.discard-popup .modal-box` (760 px), `.discard-cards` grid, `.discard-count`, `.discard-popup .discard-btn` (full-width). **Unchanged**: the message shape `{cards:[…N…], to:"discard_pile", mode:""}` — engine/AI/replay/tests are untouched. `state.mjs` also fixed a pre-existing button-state bug in the discard flow: the discard branch hides `btnPlay`, but *no* branch ever un-hid it — so after a discard pause the "Play the card" button stayed hidden in the following mana/pass and play phases (the player was left with only drag-and-drop); the `init-mana` and `mana-pass`/`play` branches now explicitly `btnPlay.classList.remove("hidden")`. `index.html` cache-bust bumped `?v=27` → `?v=28`.

- **2026-09-21 — Mages' `black_hole`.** The **fifth and final** Mages support card is now playable — **all 5 Mages cards are implemented**. `black_hole` is a **DWELLING** card (like the Engineers' `refinery` and the Doctors' `laboratory`), **not** an instant-at-play card: it is **placed** in the dwelling zone (`to: "dwelling"`, the same dispatch branch as the other dwellings) and its effect fires on a **TAP** (`mode: "dwelling_activation"`, free, once per turn). The TAP **rotates the earth 3 cells** CW or CCW to the player's choice: the 4 biomes shift position in `game.state.earth` while all tokens (players, drops, traps) stay on their cell index — so the same cell can become a different biome (changing `biome_*` conditions and the +1 faction biome bonus). The rotation is cumulative + permanent (game-level, 8 distinct states, survives a later `wrecking_ball`). Frontend changes: `cards.mjs` gained `MAGE_BLACK_HOLE` + `isMageBlackHole(id)` (and `isSupportPlay` now includes it, so the card is excluded from the defend button); `state.mjs` `selIsDwelling` now covers **all 3 dwelling types** (refinery/laboratory/black_hole — a pre-existing bug where it only checked `isEngineerDwelling` is now fixed); `comm.mjs` `sendAction` gained an optional **9th param `rotation`** (`'cw'`|`'ccw'`, sent as `msg.rotation`, omitted for every other action); `actions.mjs` `dispatchPlay` gained a **black_hole** branch (a dwelling — sends `to: "dwelling"`, the same as the refinery/laboratory) and a new **`showBlackHoleRotationPopup`** (a **CW / CCW** modal, reusing the `.pending-popup` styles); `zones.mjs` `#btn-tap` handler opens the rotation popup for a black_hole dwelling (and still sends the tap directly for refinery/laboratory, as before). **Background art rotation** (the new requirement): `board.mjs` now **pins the base earth image** to `game.state.earth_initial_b0` (fallback `game.state.earth[0][0]`) and applies a CSS `transform: rotate(earth_rotation × 15deg)` to `#board-bg` — so the background artwork **rotates** (not swaps) to match the engine's biome positions (15° per cell, the board geometry `POS24[i] = polar(-90 + (i+0.5) × 15°, 49)`). `style.css` gives `#board-bg` `transform-origin: center; transition: transform 0.6s ease` (the 0.6s transition animates the rotation on a tap). The earth rotation is verified visually via a before/after browser screenshot (the background art aligns with the biome segments after a tap). `index.html` cache-bust bumped `?v=26` → `?v=27`.

- **2026-09-20 — Mages' `Apocalypticritual`.** The fourth Mages support card is now playable (the last one, `black_hole`, was implemented next — see the 2026-09-21 entry above). It is an **INSTANT** card: playing it (move mode) opens a **cataclysm-order popup** (`actions.mjs` `showApocalypticritualPopup`, reusing the `.pending-popup` modal styles) with **4 ordered `<select>` rows** (one per biome, pre-filled with the current pile order from `game.state.cataclysm_pile` or the default `['OC','MO','DE','JU']`; each row has a swap-on-duplicate `<select>` so the 4 choices are always a valid permutation) and the chosen order is sent as `cataclysm_order` (a 4-element permutation of `OC`/`MO`/`DE`/`JU`) in the action — the engine then **sets the cataclysm pile to that order** (index 0 strikes next), permanently until the next ritual (a strong tactical lever: the player lines up which biome strikes first). Frontend changes: `cards.mjs` gained `MAGE_APOCALYPTICRITUAL` + `isMageApocalypticritual(id)` (and `isSupportPlay` now includes it, so the card is excluded from the defend button); `comm.mjs` `sendAction` gained an optional 8th param `cataclysmOrder` (sent as `msg.cataclysm_order`, omitted for every other action); `actions.mjs` `dispatchPlay` gained an **Apocalypticritual** branch (after the thermic_flux branch, before the normal move fallback) that opens the order popup and sends the choice; the biome names come from `BIOME_NAMES` (imported from `./board.mjs`). No board/zone render changes (the card is a no-op on the trip chain, advancing 0; the cataclysm pile is public board info already shown by the existing cataclysm UI). `index.html` cache-bust bumped `?v=25` → `?v=26`.
- **2026-09-19 — Mages' `thermic_flux`.** The third Mages support card is now playable (the other 2 are still no-ops). It is an **INSTANT** card: playing it (move mode) opens a **+4 °C / −4 °C popup** (`actions.mjs` `showThermicFluxPopup`, reusing the `.pending-popup` modal styles) and the chosen direction is sent as `temp_change` (`"up"`|`"down"`) in the action — the engine then **changes the planet temperature by ±4 °C, clamped to 1..20, permanently** (the `temp_inf_*` / `temp_sup_*` conditions read the new value at resolution time). Frontend changes: `cards.mjs` gained `MAGE_THERMIC_FLUX` + `isMageThermicFlux(id)` (and `isSupportPlay` now includes it, so the card is excluded from the defend button); `comm.mjs` `sendAction` gained an optional 7th param `tempChange` (sent as `msg.temp_change`, omitted for every other action); `actions.mjs` `dispatchPlay` gained a **thermic_flux** branch (after the Celestial_reversal branch, before the normal move fallback) that opens the +4/−4 popup and sends the choice. No board/zone render changes (the card is a no-op on the trip chain, advancing 0; the temperature is shown in the existing `#env-temp-val` / `#temp-badge` display, which already reads `game.state.temperature`). `index.html` cache-bust bumped `?v=24` → `?v=25`.
- **2026-09-18 — Mages' `nobodymoves`.** The second Mages support card is now playable (the other 3 are still no-ops). It is an **INSTANT** card: playing it (move mode) **locks ALL players' movement for the rest of the turn** — their MOVE cards still resolve (condition + non-movement effects fire, defend/block race still happens) but their basic advancing + movement effects are suppressed (only unstoppable may move; defend is unaffected) — the lock is cleared in the cleaning phase. Frontend changes: `cards.mjs` gained `MAGE_NOBODYMOVES` + `isMageNobodyMoves(id)` (and `isSupportPlay` now includes it, so the card is excluded from the defend button). No popup, no dispatch branch — it is played in **MOVE mode** like a normal card (falls through to the normal move-card fallback in `dispatchPlay`). No board/zone render changes (the card is a no-op on the trip chain, advancing 0).
- **2026-09-18 — Mages' `Celestial_reversal`.** The first Mages support card is now playable (the other 4 are still no-ops). It is an **INSTANT** card: playing it (move mode) opens a **Day/Night popup** (`actions.mjs` `showDayNightPopup`, reusing the `.pending-popup` modal styles) and the chosen value is sent as `day_night` (`"day"`|`"night"`) in the action — the engine then **fixes the planet's day/night for the rest of the game** (the per-turn flip is skipped). Frontend changes: `cards.mjs` gained `MAGE_CELASTIAL_REVERSAL` + `isMageCelestial(id)` (and `isSupportPlay` now includes Mages, so the card is excluded from the defend button like all support cards); `comm.mjs` `sendAction` gained an optional 6th param `dayNight` (sent as `msg.day_night`, omitted for every other action); `actions.mjs` `dispatchPlay` gained a **Celestial_reversal** branch (after the engineer/doctor branches, before the normal move fallback) that opens the day/night popup and sends the choice. No board/zone render changes (the card is a no-op on the trip chain, advancing 0). `index.html` cache-bust bumped `?v=22` → `?v=23`.
- **2026-09-17 — Earth background derives from the board's biome order.** Before, the board always showed the plain `earth_background_playmat.png`. Now `renderBoard` picks the config image matching this game's random biome order — `cards.mjs` `earthBgSrc(earth)` maps the biome of **cell 0** (segment 0 = the image's top-right quadrant, since `POS24` puts cell 0 at the top, clockwise) to `DE→cfg1, MO→cfg2, OC→cfg3, JU→cfg4` (`/assets/earth_cgf<N>_nomarker.png`); the four assets cover one starting biome each, so the starting quadrant is always drawn correctly (12 of the 24 possible board orders match 4/4 — the assets only contain 2 cyclic classes). The src is set only on actual change (~10 MB image, board re-renders per poll). No engine change: `state.earth` (public board info) already carries the biome order. `index.html` cache-bust bumped `?v=21` → `?v=22`.
- **2026-09-17 — same-turn attached pending placeholder STAYS IN PLACE.** Engine rule: when a pending card is placed this turn and then attached to a main card, its `[card, slot]` pair **stays** in `pending_slots` until the cleaning phase (the legacy behavior removed it on attachment — the freed position desynced the frontend's next-slot mirror, which offered the main card's own slot for the next play; game `8cSTX`). Frontend: `board.mjs` placeholder tooltip now distinguishes "attached" (the pair's card is an action's `pending_card`) from "waiting"; `playedCount()` needed **no change** (it counts non-null `pending_slots` entries, which now match the engine's `play_count`). `index.html` cache-bust bumped `?v=20` → `?v=21`.
- **2026-09-17 — fixed: consumed trap/drop tokens stayed on the board (visual bug).** The engine consumes drop tokens correctly (pet_trap `drop_tokens` in `apply_effect('drop')`, engineer `board_drops` in `_trigger_board_drops` — both verified against real game data and the `_pet_trap.py` / `_engineers.py` smoke tests), but the frontend **appended** `.drop-token` elements on every render and **never removed** the old ones — a token consumed by the engine kept showing on the Earth (and every poll stacked another copy of the same token). Fix: `renderBoard` now clears `#markers-layer .drop-token` before redrawing the current set (the player `.marker` persistence for the CSS slide transition is untouched). `popNewBoardTokens` still works: it runs **after** `renderAll` and targets the last-N elements of the fresh set. `index.html` cache-bust bumped `?v=19` → `?v=20`.
- **2026-09-17 — pending placeholders are `[card, slot]` pairs; attachment removes the placeholder BY CARD NAME (bug fix).** Bug (game `26_09_17_21_33_39_d5oEd`, turn 4): `p.pendings` is a **persistent** zone but `p.pending_slots` is **cleared every turn** by the cleaning phase — from turn 2 on the two lists are NOT parallel. Attaching an old pending card (low index in `pendings`) spliced `pending_slots` at that index and deleted the placeholder of a card placed the **current** turn (attaching `virus` wiped the mercurochrome placeholder → the "next stopover" hint pointed at slot 2 instead of slot 3). Fix: the engine stores `pending_slots` as `[card, slot]` pairs and attachment removes the first pair **matching the card name**; the laboratory tap records no entry. Frontend: `board.mjs` render loop iterates the entries and normalizes both shapes (pair → `[card, slot]`, legacy → `[null, slot]`, `null` slot skipped; tooltip uses the pair's card name); `actions.mjs` `playedCount()` counts the slot part of each entry (legacy `null` tap entries still skipped). `index.html` cache-bust bumped `?v=18` → `?v=19`.
- **2026-09-17 — laboratory tap is a QUICK action with NO trip-chain placeholder.** The lab tap (doctors' dwelling) still adds an `epo` to `p.pendings` (it shows in the pending-zone panel and can be attached to a main card), but the engine now records `pending_slots` with a **`null` entry** (list stays parallel to `pendings`, so the attach-removal by index is unchanged) and consumes **no position** (`play_count` untouched). Frontend: `board.mjs` already skipped `null` slots in its render loop (pairing by index stays correct); `actions.mjs` `playedCount()` now counts only **non-null** `pending_slots` entries (so a lab-tap epo no longer displaces the next play). `index.html` cache-bust bumped `?v=17` → `?v=18`.
- **2026-09-17 — pending placeholders use the dwelling placeholder art.** The doctor **pending** placeholder in a stopover slot (`.pending-placeholder`) now renders the **same faction-specific placeholder image as the dwelling cards** — `dwellingPlaceholderSrc(p.faction)` → `/cards_ex/placeholder_<key>.png` — instead of the pending card's own art (`/art/<card_name>.png`). Visual change only (`board.mjs`); the gold-border `.pending-placeholder` frame and tooltip are unchanged. `index.html` cache-bust bumped `?v=16` → `?v=17`.
- **2026-09-17 — topbar labels: turn number + connection.** `state.mjs` `renderAll` renders the topbar turn label as `Turn N (first player: <name>)` (was `Tour N`) using `st.turn_order[0]`; `comm.mjs` `setConn` shows `● connected to game <id>` (was `● connected`) when `game.id` is set.
- **2026-09-17 — dwelling placeholder now consumes a stopover position (bug fix).** Symptom: on a turn where the player placed the refinery first, the NEXT play was routed to the SAME position as the placeholder (the engine had already incremented `play_count` for the dwelling, so the engine put the next play on position 2, but the frontend still computed position 1 → the user was asked to play on stopover 1 instead of stopover 2). Root cause: `playedCount()` in `actions.mjs` only counted the `action_chain` plays — the dwelling placeholder is a separate field (`p.dwelling` + `p.dwelling_slot`), not an `action_chain` entry, so it was invisible to the position math. Fix: `playedCount()` now adds 1 when `p.dwelling && p.dwelling_slot != null` (the slot is set at placement and cleared in the cleaning phase, so it is non-null exactly while the placeholder is showing this turn). This propagates to `nextPosition()`/`nextSlotCol()`/`freeCols()`/`canDropOnStopover()`/`orderHint()` and the board slot-highlight. `index.html` cache-bust bumped `?v=15` → `?v=16`.

- **2026-09-17 — per-player stopovers.** The stopover board moved from the **shared** model (both players share 5 columns, skipping board furniture) to a **per-player** model (each player has their OWN 5 positions). A player's rooted cards occupy the leading positions of that player's chain (position 1, 2, …) and the player's plays go after them (position R+1, …); a player's rooted cards never shift the opponent's positions. A rooted card on the trip chain is now a **normal card** (basic advancing only, no condition/effect, but **blockable** by the opponent's defend at the same per-player position). Frontend: `actions.mjs` gained `rootedCount()`/`nextPosition()` and `freeCols()`/`nextSlotCol()`/`occupiedCols()` are now per-player (they take the player name); `canDropOnStopover`/`orderHint`/`dispatchPlay` use the per-player rule; `board.mjs` slot-highlight + `game.mjs`/`zones.mjs` guards pass the player name to `freeCols`. The engine is the source of truth (`ge._player_stopover`/`ge._player_chain`; it overwrites the client's `to`); the robot calls it directly (`ai_driver.py`). Mirrors `ge._player_stopover`.
